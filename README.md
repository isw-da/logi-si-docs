# SI / Composer docs mirror

Local snapshot of insightsoftware product documentation for LLM ingestion
(retrieval store, knowledge files, offline reference). Not for public republication.

## simba-intelligence/
Pulled from https://insightsoftware.mintlify.app (Mintlify, LLM-friendly).
- `llms-full.txt`  full SI doc corpus in one file
- `llms.txt`       index of all pages
- `openapi.json`   PLACEHOLDER ONLY (Mintlify sample, paths /plants) — not the real SI API
- `pages/`         27 doc pages as clean markdown

Refresh: re-run the curl pull against the same base URLs.
